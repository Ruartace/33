### 实时搜索接口（GET `/api/ruidao/realtime-search`）

> 说明：应用配置了 `server.servlet.context-path=/api`，控制器类上写 `@RequestMapping("/ruidao")`，最终实际访问路径为 `/api/ruidao/realtime-search`。

- **用途**：支持多字段模糊/区间实时查询 `RuiDao` 记录，结果按 `id` 倒序返回。
- **请求方式**：GET
- **请求头**：`Content-Type: application/json`


---

### 请求体（`RuiDaoSearchRequest`）
> 所有字段均可选；传 `null` 或空字符串会忽略该条件，全部字段为空时返回全部数据。

| 字段 | 类型 | 是否必填 | 说明 |
|---|---|---|---|
| `projectNo` | string | 否 | 项目编号，模糊匹配 |
| `applicationNo` | string | 否 | 申请号，模糊匹配 |
| `caseName` | string | 否 | 案件名称，模糊匹配 |
| `customerName` | string | 否 | 客户名称，模糊匹配 |
| `notificationName` | string | 否 | 通知书名称，模糊匹配 |
| `applicationType` | string | 否 | 申请类型，模糊匹配 |
| `status` | string | 否 | 状态，精确匹配（见状态枚举） |
| `preliminaryCase` | string | 否 | 是否预审案，精确匹配 |
| `priorityExamination` | string | 否 | 是否优审案，精确匹配 |
| `institutionNumber` | string | 否 | 机构编号，模糊匹配 |
| `officialDocumentDateStart` | string (yyyy-MM-dd) | 否 | 官文日期起始，`>=` |
| `officialDocumentDateEnd` | string (yyyy-MM-dd) | 否 | 官文日期结束，`<=` |
| `applicationDateStart` | string (yyyy-MM-dd) | 否 | 申请日期起始，`>=` |
| `applicationDateEnd` | string (yyyy-MM-dd) | 否 | 申请日期结束，`<=` |

**过滤规则：**
- 模糊字段使用 `LIKE %value%`
- 日期区间分别使用 `>=` 和 `<=` 比较
- `status`、`preliminaryCase`、`priorityExamination` 采用等值匹配
- 结果按 `id DESC` 排序

**`status` 枚举值：**

| 值 | 含义 |
|---|---|
| `未处理` | 用户上传解析完 ZIP 文件后的初始状态 |
| `匹配失败` | 匹配项目功能执行后，数据未能完全匹配上 |
| `匹配成功` | 匹配项目功能执行后，所有数据完全匹配成功 |
| `已处理` | 数据已成功导入系统 |
| `无需处理` | 用户自定义状态，后续功能将跳过此数据 |

---

### 成功响应
- **HTTP 200**，响应体结构遵循 `ApiResponse<List<RuiDaoResponse>>`：
```json
{
  "success": true,
  "message": "OK",
  "data": [ { ... } ]
}
```

### 响应数据字段（`RuiDaoResponse`）
| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | number | 主键 ID |
| `projectNo` | string | 项目编号 |
| `caseCode` | string | 案件编码 |
| `applicationNo` | string | 申请号 |
| `caseName` | string | 案件名称 |
| `sourceType` | string | 来文类型 |
| `officialDocumentDate` | string (yyyy-MM-dd) | 官文日期 |
| `notificationBrief` | string | 通知书摘要 |
| `notificationCode` | string | 通知书编码 |
| `internalCode` | string | 内部代码 |
| `documentSequenceNumber` | string | 发文序列号 |
| `notificationName` | string | 通知书名称 |
| `applicationDate` | string (yyyy-MM-dd) | 申请日期 |
| `applicationType` | string | 申请类型 |
| `priorityExamination` | string | 是否优审案 |
| `priorityLevel` | string | 优先级别 |
| `preliminaryCase` | string | 是否预审案 |
| `institutionNumber` | string | 机构编号 |
| `customerName` | string | 客户名称 |
| `status` | string | 状态 |
| `fileCategoryMajor` | string | 文件大类 |
| `fileCategoryMinor` | string | 文件小类 |
| `caseId` | number | 关联案件 ID |

---

### 代码实现（示例）
```java
package com.ruidao.pdfocrservice.controller;

import com.ruidao.pdfocrservice.dto.ApiResponse;
import com.ruidao.pdfocrservice.dto.RuiDaoResponse;
import com.ruidao.pdfocrservice.dto.RuiDaoSearchRequest;
import com.ruidao.pdfocrservice.service.RuiDaoSearchService;
import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.ModelAttribute;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.List;

@RestController
@RequestMapping("/ruidao") // 配合 context-path=/api，最终路径 /api/ruidao/**
@RequiredArgsConstructor
public class RuiDaoController {

    private final RuiDaoSearchService ruiDaoSearchService;

    @GetMapping("/realtime-search")
    public ApiResponse<List<RuiDaoResponse>> realtimeSearch(
            @ModelAttribute RuiDaoSearchRequest request
    ) {
        List<RuiDaoResponse> result = ruiDaoSearchService.search(request);
        return ApiResponse.ok(result);
    }
}
```


---

### 示例
**请求（GET 查询参数）**
```
/api/ruidao/realtime-search?projectNo=XM2024&applicationNo=202310123456&caseName=一种节能设备&customerName=示例公司&officialDocumentDateStart=2024-01-01&officialDocumentDateEnd=2024-12-31&applicationDateStart=2023-01-01&applicationDateEnd=2023-12-31&notificationName=发明专利申请&status=已处理&preliminaryCase=Y&priorityExamination=N&institutionNumber=A123&applicationType=发明
```

**响应（示例）**
```json
{
  "success": true,
  "message": "OK",
  "data": [
    {
      "id": 12,
      "projectNo": "XM2024-001",
      "caseCode": "RD-2024-001",
      "applicationNo": "202310123456.7",
      "caseName": "一种节能设备",
      "sourceType": "通知书",
      "officialDocumentDate": "2024-03-18",
      "notificationBrief": "受理通知",
      "notificationCode": "N001",
      "internalCode": "I001",
      "documentSequenceNumber": "SEQ-001",
      "notificationName": "发明专利申请受理通知书",
      "applicationDate": "2023-09-10",
      "applicationType": "发明",
      "priorityExamination": "N",
      "priorityLevel": null,
      "preliminaryCase": "Y",
      "institutionNumber": "A123",
      "customerName": "示例公司",
      "status": "已处理",
      "fileCategoryMajor": "专利",
      "fileCategoryMinor": "发明",
      "caseId": 100
    }
  ]
}
```

---

### 导出接口（GET `/api/ruidao/realtime-search/export`）

- **用途**：按主键 ID 导出实时搜索结果为 Excel，字段固定与 `RuiDaoResponse` 对应，不支持字段选择；支持导出单条或多条。
- **请求方式**：GET
- **请求头**：`Content-Type: application/json`（查询参数传递），`Accept: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`

**请求参数（必填）**
- `ids` (array<number>)：要导出的 `RuiDao` 记录主键 ID 数组，至少包含 1 个 ID。

**响应**
- **HTTP 200**，返回 Excel 二进制流（多条记录则多行）。
- `Content-Type`: `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`
- `Content-Disposition`: `attachment; filename="zhuanli-yyMMdd.xlsx"; filename*=UTF-8''专利来文yyMMdd.xlsx`


**示例**
```
# 单条
/api/ruidao/realtime-search/export?ids=123

# 多条
/api/ruidao/realtime-search/export?ids=123&ids=456&ids=789
```



